# API reference

Base URL in local development: `http://localhost:4000`.

## Live routes

| Method | Path                                   | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ------ | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GET    | `/health`                              | Returns API health information.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| POST   | `/auth/login`                          | Staff login step 1. Requires `email` and a password of at least 12 characters. Returns `{mfaRequired, enroll, mfaToken}` (10-minute); audit-logs failures and locks after 5 fails/15min.                                                                                                                                                                                                                                                                                                                                |
| POST   | `/auth/mfa/enroll`                     | Starts TOTP pairing (`{mfaToken}`); returns secret + otpauth URL + QR data URL once.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| POST   | `/auth/mfa/confirm`                    | Confirms pairing (`{mfaToken, code}`); enables MFA and returns JWT access (30m) and refresh (7d) tokens.                                                                                                                                                                                                                                                                                                                                                                                                                |
| POST   | `/auth/mfa/verify`                     | Daily TOTP check (`{mfaToken, code}`); returns JWT access and refresh tokens.                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| POST   | `/auth/refresh`                        | Rotates refresh token; refuses tokens issued before a session revocation.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| POST   | `/auth/logout`                         | Clears the refresh-token hash (idempotent).                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| POST   | `/auth/invite`                         | Super-admin only. Creates a pending account with `{fullName, email, role}` (`role` in `ADMIN/FULFILLMENT/CS`, default `FULFILLMENT`). Email-enabled envs queue a `STAFF_INVITATION` outbox email and return `{id, emailed:true}` (no token); otherwise returns a 48h `setupToken` for local setup.                                                                                                                                                                                                                      |
| POST   | `/auth/invite/:id/resend`              | Super-admin only. Regenerates the setup token, drops stale pending invite emails, and re-queues (or returns the token when email is disabled).                                                                                                                                                                                                                                                                                                                                                                          |
| POST   | `/auth/setup`                          | Invited member sets their own password (`{token, password}`); returns an MFA token for pairing.                                                                                                                                                                                                                                                                                                                                                                                                                         |
| GET    | `/staff/orders`                        | Open fulfillment queue (`search`, `status`, `certificate`, `rushOnly`, `assigned=mine\|unassigned\|all`, `openOnly`, `attentionFirst`, `page`). All roles see all paid orders (rows masked for non-owners); `assigned` narrows. `attentionFirst` sorts parked orders, then rush, then oldest.                                                                                                                                                                                                                           |
| GET    | `/staff/analytics`                     | Audit-derived analytics for the authenticated staff member only. Supports the admin analytics date, workflow, and pagination parameters; the staff identity always comes from the access token.                                                                                                                                                                                                                                                                                                                         |
| POST   | `/staff/orders/:id/claim`              | Atomic exclusive claim of a paid unassigned order (409 if taken).                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| POST   | `/staff/orders/:id/release`            | Owner or super-admin releases an order to the queue.                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| POST   | `/staff/orders/:id/reassign`           | Super-admin only. Moves an order to an active agent (`{staffId}`).                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| GET    | `/staff/orders/:id`                    | Full detail for owner-agent or ADMIN only (CS opens owned orders like fulfillment); SSN/card stay `*********`. `pricing` + `amountCents` are returned only to `ADMIN`/`CS`.                                                                                                                                                                                                                                                                                                                                             |
| POST   | `/staff/orders/:id/notes`              | Internal note (1–2000 chars) by owner or ADMIN; never shown to customers.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| PATCH  | `/staff/orders/:id/correction`         | CS-owner or ADMIN. EDIT-only fix of the whole application form (`applicant`, `subject`, `family`, `addresses`, `geo`, `reason`, `deliveryMethod`, `destinationType`, `requestorSsn`, `paymentCard`, optional `note`) — CS must claim first. Merged values validated like a new submission; failures return 422 `{message, errors}` keyed for inline display. SSN/card are encrypted into `confidentialData` and audited by field name only.                                                                             |
| PATCH  | `/orders/:id/status`                   | Staff fulfillment flow `PAID → IN_REVIEW → SUBMITTED`, plus `IN_REVIEW → TO_CS` (required internal note, optional substatus, auto-releases for CS to claim) and CS-lane `TO_CS → GTG` (CS-owner or ADMIN, note is optional, clears the substatus, drops ownership back to the pool) → `GTG → IN_REVIEW` (owner or ADMIN). Nothing leaves `TO_CS` except `GTG`. Owner-agent or super-admin otherwise; paid orders only. Landing on `SUBMITTED` queues one `SUBMISSION_NOTIFICATION` customer email (email-enabled envs). |
| POST   | `/orders/:id/document`                 | Owner-agent or ADMIN. Uploads (or replaces) the single completion PDF (multipart `file`, PDF-only, 10 MB max, magic-byte checked); old chunks are deleted. Fulfillment needs it attached before `SUBMITTED`.                                                                                                                                                                                                                                                                                                            |
| GET    | `/orders/:id/document`                 | Owner-agent or ADMIN. Streams the completion PDF as an attachment; the download is audit-logged.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| DELETE | `/orders/:id/document`                 | Owner-agent or ADMIN. Deletes the completion PDF and its metadata; audit-logged.                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| GET    | `/admin/staff`                         | Super-admin roster with workload counts (no secrets).                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| GET    | `/admin/staff/:id/analytics`           | Super-admin-only, audit-derived staff analytics. Accepts optional `from`/`to` (`YYYY-MM-DD`), `status=all\|processing\|to_cs\|gtg\|submitted`, `page`, and `limit`; returns safe staff identity, unique-form KPIs, and paginated masked order metadata only.                                                                                                                                                                                                                                                            |
| GET    | `/admin/order-activity`                | Super-admin order activity index with order-number search and pagination. Returns safe order metadata, total audit-event count, and latest activity timestamp.                                                                                                                                                                                                                                                                                                                                                          |
| GET    | `/admin/order-activity/:id`            | Super-admin safe activity timeline for one order, with staff actor identity and sanitized status/field metadata only.                                                                                                                                                                                                                                                                                                                                                                                                   |
| PATCH  | `/admin/staff/:id`                     | Super-admin renames, disables, or changes `role` (`ADMIN/FULFILLMENT/CS`; role change revokes sessions; last active ADMIN cannot be demoted; self-change blocked).                                                                                                                                                                                                                                                                                                                                                      |
| POST   | `/admin/staff/:id/revoke`              | Super-admin revokes all sessions immediately.                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| POST   | `/admin/staff/:id/mfa-reset`           | Super-admin clears TOTP pairing (forces re-enrollment) + revokes sessions; audited.                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| GET    | `/admin/workload`                      | Per-agent active/completed order counts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| GET    | `/admin/activity`                      | Combined staff + order activity feed (`staffId`, `action`, `limit`); no secrets.                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| POST   | `/contact-messages`                    | Stores a public Contact Us inquiry and queues a support notification plus a customer receipt.                                                                                                                                                                                                                                                                                                                                                                                                                           |
| POST   | `/orders`                              | Creates an unpaid order with the full application. Validates per-cert fields, geo, consents, and server-calculated total.                                                                                                                                                                                                                                                                                                                                                                                               |
| POST   | `/orders/verify-before-payment`        | Dry-run validation; returns canonical `amountCents` without writing.                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| GET    | `/orders/geo/:stateCode?county=&city=` | Validates a county/city pair against the dataset.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| GET    | `/orders/checkout-config`              | Returns the Stripe publishable key (browser-safe).                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| GET    | `/orders/:id/summary`                  | Whitelisted order summary for checkout (no SSN/card).                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| POST   | `/orders/:id/checkout-session`         | Creates (or reuses) a Stripe Checkout Session (`ui_mode: elements`); server-computed total only.                                                                                                                                                                                                                                                                                                                                                                                                                        |
| POST   | `/orders/checkout-session/confirm`     | Verifies a session with Stripe; marks the order paid.                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| POST   | `/orders/:id/reveal`                   | Staff-only audited reveal of SSN or card (`{field: ssn\|card, reason}`); assigned agent or super-admin; rate-limited. Returns plaintext once and audit-logs the reveal.                                                                                                                                                                                                                                                                                                                                                 |
| GET    | `/orders/:id/audit`                    | Staff-only audit history; owner or ADMIN only. Events never contain secrets.                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| POST   | `/orders/tracking`                     | Public lookup by `publicNumber` and `email`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| POST   | `/webhooks/stripe`                     | Stripe-only signed webhook endpoint. Paid events atomically queue one confirmation email plus, when enabled, one GA4 purchase and one OpenAI `order_created` conversion (idempotent per order); it is not a browser API.                                                                                                                                                                                                                                                                                                |

## Order creation contract

`POST /orders` accepts the full application (see `lib/order-validation.ts`
`createOrderSchema`): state slug/code/name, certificate, geo county/city,
reason, applicant, optional `requestorSsn`, per-cert `subject`/`family`,
home/shipping/billing `addresses`, destination type, copies 1–20, rush,
delivery method, consents + payment authorization, signature, card details, and display
`totalCents` (recomputed server-side as processing + rush only; mismatch is rejected with 422).

It returns an order id, a public order number, and the server-calculated `amountCents`. The browser must not supply an amount. SSN and payment-card details are encrypted (AES-256-GCM) into `confidentialData` before persistence and never appear in responses. Staff reveal them only through `POST /orders/:id/reveal` with a recorded reason.

## Contact message contract

`POST /contact-messages` accepts `fullName`, `email`, optional `orderNumber`,
`message`, and a contact-only `antiAbuse` object containing a honeypot and form
start timestamp. The route allows five submissions per IP every 15 minutes.
Likely SSN/card-number content is flagged for staff but does not block the
message. There is intentionally no public endpoint to retrieve messages.

## Security contract

- Application card/SSN fields are encrypted into `confidentialData` at order creation; plaintext values are never logged, drafted, or returned except once via the authorized reveal endpoint. Never send Stripe secrets to these routes.
- The public tracking route only returns the matching order; future hardening should explicitly project a minimal public-safe response.
- Stripe events are stored by event ID so retries cannot update an order twice.
